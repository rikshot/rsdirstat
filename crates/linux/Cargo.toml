[package]
name = "rsdirstat-linux"
version = "0.1.0"
edition.workspace = true

[dependencies]
rsdirstat-core = { path = "../core" }
anyhow = "1"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
dashmap = { version = "6", features = ["inline"] }
