[package]
name = "rsdirstat-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
mime = "0.3"
mime_guess = "2"

[target.'cfg(unix)'.dependencies]
libc.workspace = true
