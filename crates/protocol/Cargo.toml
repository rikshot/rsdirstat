[package]
name = "rsdirstat-protocol"
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Wire protocol shared between the rsdirstat server and its WASM frontend"
categories = ["encoding"]

[dependencies]
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }
miniz_oxide = "0.9"
